# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / web build
web/node_modules/
web/dist/

# Rebuildable artifacts (regenerate with: draft.bat build)
data/draft.db

# Collected match outcomes (re-collectable via lol_draft.collect; can be large,
# and labels are tied to a key/region — keep out of git)
data/games/

# Trained model artifacts (rebuildable via lol_draft.train from data/games/)
data/models/

# OS junk
Thumbs.db
.DS_Store

# Keep: data/raw/* (source data) and web/src/mock/champions.ts (generated, committed)
